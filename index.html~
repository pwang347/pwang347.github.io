---
layout: default
---


<!-- Main -->
<div id="main">
    <section>
            <header class="major">
                <h2>About Me</h2>
            </header>
		<p> I'm currently a first year student completing a Computer Science degree at the University of British Columbia.
		</p>
		<p> I like to make apps, draw and design my own video games.
		</p>
    </section>

    <section>
	<div>
	    <header class="major">
                <h2>Portfolio</h2>
            </header>
	    <p> Below are some of the projects I've been working on:
	    </p>	
	</div>
        <div class="row">
            {% for post in site.posts %}

                <article class="{% cycle '6u', '6u$' %} 12u(small)">
                		<center>{{ post.title }}</center>    
		{% assign images = post.content | split:"<img " %}
			{% for image in images %}
  			{% if image contains 'src=' %}
    			{% assign imageMarkup = image | split:"class" | first %}
   			 <a href={{post.url}} class="image fit thumb"><img {{ imageMarkup }}></a> 
   			 {% break %}
 			 {% endif %}
		{% endfor %}

                </article>

            {% endfor %}
        </div>
    </section>
</div>
